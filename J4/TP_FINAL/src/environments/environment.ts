export const environment = {
  production: false,
  apiBaseUrl: 'https://rickandmortyapi.com/api',
  /** En dev : proxy Angular (proxy.conf.json) évite le CORS sur GraphQL. */
  graphqlUrl: '/graphql',
};
